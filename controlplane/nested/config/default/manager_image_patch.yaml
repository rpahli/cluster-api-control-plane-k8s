apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: test.io/nested-controlplane-controller-amd64:dev
        name: manager
        imagePullPolicy: Never  # or IfNotPresent
